services:
  - type: web
    name: gateway-checker-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python src/gateway_checker_bot.py"
    pythonVersion: 3.10.12
